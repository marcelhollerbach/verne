class Elm.File.Detail(Elm.Layout) {
    methods {
        @property file {
            values {
                file : Efm.File;
            }
        }
        @property cache {
            values {
                cache : Elm.File.MimeType.Cache;
            }
        }
        rename {

        }
    }
    implements {
        Efl.Object.constructor;
        class.constructor;
    }
}