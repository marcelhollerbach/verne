class Elm.File.Detail(Elm.Layout) {
    methods {
        @property file {
            values {
                file : Efm.File*;
            }
        }
        @property cache {
            values {
                cache : Elm.File.MimeType.Cache*;
            }
        }
    }
    implements {
        Evas.Object_Smart.add;
        class.constructor;
    }
}