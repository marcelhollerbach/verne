class Elm.File.Detail(Efl.Ui.Layout) {
    methods {
        @property file {
            values {
                file : Efm.File;
            }
        }
        @property cache {
            values {
                cache : Elm.File.MimeType.Cache;
            }
        }
        rename {

        }
    }
    implements {
        Efl.Object.constructor;
        class.constructor;
    }
}
