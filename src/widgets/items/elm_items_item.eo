class Elm.Items.Item (Elm.Widget) {
    methods {
        @property content {
            values {
                content : Evas.Object*;
            }
        }
        @property selected {
            values {
                selected : bool;
            }
        }
        realize {

        }
        unrealize {

        }
        item_get {
            return : Efl.Tree.Base*;
        }
        @property search {
            [[Elm.Items.Display supports seraching, this property is the way how it compares this item to the others]]
            values {
                string: const(char)*;
            }
        }
    }
    implements {
        Evas.Object_Smart.add;
        Eo.Base.destructor;
    }
    events {
        realize;
        unrealize;
        selected;
        unselected;
        clicked;
        clicked,double;
    }
}