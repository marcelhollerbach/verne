enum Elm.Items.Move_Dir {
    NORTH, WEST, EAST, SOUTH
}

abstract Elm.Items.Display (Elm.Scroller) {
    [[]]
    methods {
        @property tree {
            get {

            }
            values {
                root : Efl.Tree.Base*;
            }
        }
        sel_move {
            params {
                direction : Elm.Items.Move_Dir;
            }
        }
        select {

        }
        item_search_xywh {
            params {
                x : int;
                y : int;
                w : int;
                h : int;
            }
            return : list<Elm.Items.Item*>*;
        }
        search {
            params {
                search : const(char)*;
            }
        }
        @property selected {
            get {

            }
            values {
              selected : list<Elm.Items.Item*>*;
            }
        }
        @property realizes {
            values {
              realizes : list <Elm.Items.Item*>*;
            }
        }
    }
    implements {
        @virtual .sel_move;
        @virtual .select;
        Elm.Widget.event;
        Eo.Base.constructor;
        Eo.Base.destructor;
    }
}